[project]
name = "api-xavier"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
   "fastapi[standard]==0.138.2",
   "pydantic==2.13.4",
   "python-dotenv==1.2.2",
   "supabase==2.5.1"
]

[tool.fastapi]
entrypoint = "main:app"